name: Building Codelab from "codelab.md"

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    paths: 'codelab.md'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5.0.0
      - name: Installing claat @ latest
        run: go install github.com/googlecodelabs/tools/claat@latest
      - name: Making the HTML
        run: claat export codelab.md
      - name : Git add everything
        run : |
          cd codelab-4-codelab-markdown
          ls
      # - name: Setup git user
      #   run: |
      #     git config --global user.name "nischalon10"
      #     git config --global user.email "nischalon10@gmail.com"
      # - name : Git Checkout to the pages branch
      #   run : git checkout pages || git checkout -b pages
      # - name : Git Commit
      #   run : git commit -m "Testing"
      # - name : Gti Push
      #   run : git push --set-upstream origin pages
      
        

     
